#!/bin/bash
#
# TODO: move beyond dev mode

set -euo pipefail

kugl_home=$(dirname "$0")/..
. $kugl_home/venv//bin/activate
PYTHONPATH=$kugl_home

exec python3 -c 'if True:
    import sys
    from kugl.main import main
    main()
' "$@"

